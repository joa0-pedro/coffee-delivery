import styled from "styled-components";

export const InfoContent = styled.div`
		display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    align-items: center;
    margin-bottom: 2rem;
`
export const Info = styled.div`
		display: flex;
		flex-direction: column;
				flex-wrap: wrap;
`
export const TextItems = styled.div`
display:flex;
flex-direction: column;
flex-wrap: wrap;

 p:nth-child(2) {font-weight: 700; }
`

